const { verifySession } = require('../../lib/auth'); const {octokit,owner,assertConfig}=require('../../lib/github');
module.exports=async(req,res)=>{if(!(await verifySession(req)))return res.status(401).json({error:'Unauthorized'});try{assertConfig();if(req.method==='GET'){const r=await octokit.repos.listForUser({username:owner,per_page:100,sort:'updated'});return res.json(r.data.map(x=>({name:x.name,full_name:x.full_name,private:x.private,default_branch:x.default_branch,description:x.description,html_url:x.html_url,updated_at:x.updated_at})));}if(req.method==='POST'){const b=req.body||{};if(!b.name)return res.status(400).json({error:'name required'});const r=await octokit.repos.createForAuthenticatedUser({name:b.name,description:b.description||'',private:!!b.private,auto_init:!!b.auto_init});return res.status(201).json(r.data);}return res.status(405).json({error:'Method not allowed'});}catch(e){res.status(e.status||500).json({error:e.message});}};
